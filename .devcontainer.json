{
  "tasks": {
    "test": "ls backend && ls frontend",
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
    "launch": "cd backend && npm install && npm start && cd ../frontend && npm install && npm start"
  }
}